# /etc/rsyslog.d/10-loki-forward.conf
module(load="imudp")
input(type="imudp" port="514")

*.* action(
    type="omfwd"
    target="127.0.0.1"
    port="1514"
    protocol="udp"
    Template="RSYSLOG_SyslogProtocol23Format"
)
